using UnityEngine;

public class RotateItem : MonoBehaviour
{
    public GameObject heldObject;
    public float rotationSpeed = 1f;

    // Update is called once per frame
     public void Rotate(GameObject heldObject)
    {
         //rotate
        
    }
}
